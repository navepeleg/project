name: Terraform

on:
  workflow_dispatch:
    input:
      eks-name:
        description: 'EKS Cluster Name'
        default: 'Fargate'
        required: false
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: run hello world
        run: echo 'Hello World!'